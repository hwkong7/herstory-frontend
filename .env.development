VITE_API_BASE_URL=https://herstory-backend.onrender.com
VITE_KAKAO_REDIRECT_URI=http://localhost:5173/oauth/callback/kakao
VITE_GOOGLE_REDIRECT_URI=http://localhost:5173/oauth/callback/google

# AI 프리뷰 생성용 OpenAI API 키. 절대 여기(.env.development)에 실제 값을 넣지 말 것 —
# 이 파일은 git에 커밋됨. 실제 키는 .env.development.local 파일을 새로 만들어 거기에만 넣을 것
# (해당 파일은 .gitignore의 *.local 규칙으로 이미 커밋 대상에서 제외됨).
VITE_OPENAI_API_KEY=
